#include <stdio.h>
#include <stdlib.h>

int main()
{
    int selecao;
    float unit;
    printf("Digite 1 para libras pra kg e 2 para milhas para km !\n");
    scanf("%d", &selecao)
    if (selecao ==1 )
        printf("%f", (unit*0,453592)
    return 0;
}
